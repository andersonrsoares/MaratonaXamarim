<?xml version="1.0" encoding="utf-8"?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms" xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml" xmlns:local="clr-namespace:FormAssistControl" x:Class="FormAssistControl.FormAssistControlPage">

	<StackLayout Padding="0,20,0,0">
		<Button Text="Carregar diretorio" Command="{Binding LoadDirectoryCommand}"></Button> 
		<ActivityIndicator IsRunning="{Binding IsBusy}" IsVisible="{Binding IsBusy}"></ActivityIndicator>	
			<ListView ItemsSource="{Binding Students}" x:Name="lvStudents">
				<ListView.ItemTemplate>
					<DataTemplate>
						<ImageCell Text="{Binding Name}" Detail="{Binding Average}"></ImageCell>
					</DataTemplate>
				</ListView.ItemTemplate>
			</ListView>
	</StackLayout>

</ContentPage>
